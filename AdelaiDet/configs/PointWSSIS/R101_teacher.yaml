_BASE_: "Base-PointWSSIS.yaml"
MODEL:
  WEIGHTS: "detectron2://ImageNetPretrained/MSRA/R-101.pkl"
  RESNETS:
    DEPTH: 101
  SOLVER:
    IMS_PER_BATCH: 8  # Reduced from 16
    AMP:
      ENABLED: True   # Use mixed precision
  INPUT:
    MIN_SIZE_TRAIN: (512,)  # Reduced from (640, 672, 704, 736, 768, 800)
    MAX_SIZE_TRAIN: 1024     # Reduced from 1333